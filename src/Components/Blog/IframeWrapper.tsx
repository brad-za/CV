import React from "react";

interface IframeWrapperProps {
  id: string;
}

const IframeWrapper: React.FC<IframeWrapperProps> = ({ id }) => {
  // Placeholder component - react-gist dependency removed
  // Returns null to prevent crashes if old content references this
  return null;
};

export default IframeWrapper;
